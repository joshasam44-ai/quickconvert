# quickconvert

- Use the `src/` layout and keep the package dependency-free unless a dependency is necessary.
- Run `python -m unittest discover -s tests -v` before submitting changes.
- Keep public APIs documented with docstrings and covered by focused tests.
